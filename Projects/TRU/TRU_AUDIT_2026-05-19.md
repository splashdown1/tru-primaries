# tru audit — 2026-05-19

## scope

audited tru-related files under `/home/workspace`, excluding `Trash/` and dependency noise under `node_modules/`.

- relevant files found: 104
- total size: ~91 mb
- major formats: 42 html, 11 json, 8 b64, 8 txt, 6 md, 6 py, 14 mjs

## current truth

`TRU_SUPER.html` is the current working head.

why:
- newest user-facing build: 2026-05-20 00:13 utc
- loads successfully in browser
- 31,015 nodes embedded
- fully offline, no api key required
- arithmetic works
- kjv direct lookup works, e.g. `john 3:16`
- exact recall works, e.g. `what is falsifiability`
- local learning/export/reset/stats commands exist

main caveat: `TRU_SUPER_patched.html` differs only in teaching syntax. patched accepts natural `x is y`; current `TRU_SUPER.html` requires `teach x is y`. decide which behaviour should become canonical.

## phase map

| era | files | status |
|---|---|---|
| early solar/offline/v46-v66 | top-level `tru_*`, `TRU_CORE/*`, `TRU_ALIVE*`, `TRU_GHOST.html`, `TRU_SELF_WRITING.html` | archive/history |
| phase 6-16 | `Projects/TRU/TRU_PHASE6_UNIFIED.html` through `TRU_PHASE16_UNIFIED.html` | superseded; phase 16 was once “definitive” |
| scripture line | `TRU_Phase19_Scripture_Offline.html`, `Projects/TRU/TRU_Phase25_Offline.html`, `Projects/TRU/TRU_Phase26_Offline.html` | promising, but phase 26 has runtime/build issues |
| super line | `TRU_SUPER.html`, `TRU_SUPER_patched.html`, `TRU_MEGA_BRAIN.json`, `TRU_SUPER_BRAIN.json` | current best direction |

## important files

### current candidates

- `TRU_SUPER.html` — best working app, 31,015 nodes.
- `TRU_SUPER_patched.html` — same app except more permissive teaching syntax.
- `TRU_MEGA_BRAIN.json` — 31,015-node source brain; includes full kjv plus training/embedded/json knowledge.
- `TRU_SUPER_BRAIN.json` — smaller 1,170-node curated brain.
- `Projects/TRU/data/tru_super_brain.js` — 1,584-node js export.

### older but useful

- `Projects/TRU/TRU_PHASE16_UNIFIED.html` — clean “living book” baseline, smaller and documented.
- `Projects/TRU/TRU_PHASE14_UNIFIED.html` — visual/art-heavy backup.
- `Projects/TRU/data/kjv_full.json`, `kjv_ot_english.json`, `kjv_nt_greek_english.json` — scripture source data.
- `Projects/TRU/micro/*.mjs` + `session.mjs` — original modular engine pieces.

### docs that are stale

- `Projects/TRU/README.md` still says phase 7.
- `Projects/TRU/FINAL_SUMMARY.md` says phase 14/15 are “the one”.
- `Projects/TRU/MISSION_REPORT.md` and `MISSION_COMPLETE.md` say phase 16 is definitive.

all are historically useful, but no longer describe the current head.

## problems found

### 1. canonical source is unclear

current best app is at workspace root (`TRU_SUPER.html`), while most project source lives in `Projects/TRU/`.

risk: future work will patch the wrong file.

### 2. phase 26 is broken as a brain app

`Projects/TRU/TRU_Phase26_Offline.html` opens, but status shows:

> tru ready — 0 nodes | ot 41 books | nt 27 books

cause: generated html calls `init()` before pako loads, so brain decompression fails and `S.brain` becomes empty.

also, scripture search is text-match only. `john 3:16` returns early john matches instead of the exact verse.

### 3. phase 26 builder is not reproducible

`Projects/TRU/build_p26.py` depends on missing temporary paths:

- `/tmp/trubrain_api.json`
- `/home/.z/workspaces/con_MYlif5vQjCGGJeL3/sblgnt_nt_combined.txt`

risk: cannot rebuild phase 26 cleanly from workspace alone.

### 4. project root is messy

there are many historical html builds at `/home/workspace` plus `Projects/TRU/`. duplicates found:

- `tru-offline-dir/index.html` = `tru-offline.html`
- `tru-solar-new/index.html` = `tru-solar.html`
- `TRU_ALIVE.html` = `TRU_ALIVE_v1.html`
- `tru-online-dir/index.html` = `tru-online.html`

### 5. accidental workspace dependencies

root `package.json`, `package-lock.json`, and `node_modules/` only contain `jsdom`. likely created for testing/patching, not part of tru proper.

### 6. no obvious live secret leak found

scanned for obvious `sk-*` and explicit api-key assignments. no actual secret-looking key literals found. several files mention api/deepseek/token concepts, but not hard-coded secret values.

## where we are

tru has crossed from prototype into “needs consolidation”.

it is no longer missing a brain. it has too many brains, too many heads, and no single build path.

best current state:

- working app: `TRU_SUPER.html`
- best brain: `TRU_MEGA_BRAIN.json`
- best polished scripture ui idea: `Projects/TRU/TRU_Phase26_Offline.html`, but needs repair
- best modular base: `Projects/TRU/micro/` + `session.mjs`, but not used by current super build

## where to go next

### phase 27 should be consolidation, not expansion

recommended sequence:

1. create `Projects/TRU/current/`
   - move/copy canonical `TRU_SUPER.html` there as `index.html`
   - store canonical brain as `brain.json`
   - keep one builder script that can recreate the html from source files

2. fix `TRU_SUPER.html`
   - decide teaching syntax: require `teach x is y` or allow natural `x is y`
   - add exact bible book/chapter/verse handling for all common forms
   - improve retrieval so bible/system-log nodes do not swamp general answers
   - add a small test harness for arithmetic, bible lookup, exact recall, gap, teach/export

3. salvage phase 26 scripture ui
   - move pako script before main script or remove pako dependency
   - fix exact reference lookup before fuzzy scripture search
   - rebuild from workspace-local data only
   - merge the good scripture ui into super only after tests pass

4. clean the repo
   - mark old builds as archive
   - update README to say current head is super/phase 27
   - delete or move accidental `node_modules/` if no longer needed
   - preserve old html builds but stop treating them as active

5. publish after consolidation
   - either upload `TRU_SUPER.html` as a shared file collection via zo.pub, or make a dedicated zo site/page if you want it browsable as a proper app

## recommended immediate action

build `phase 27 — canonical super tru`:

- source of truth: `TRU_MEGA_BRAIN.json`
- app shell: `TRU_SUPER.html`
- patch: carry over natural teaching from `TRU_SUPER_patched.html` if desired
- repair: exact bible lookup and retrieval ranking
- output: `Projects/TRU/current/index.html`
- docs: rewrite `Projects/TRU/README.md` around current reality

short version: tru is alive. now it needs a spine.
